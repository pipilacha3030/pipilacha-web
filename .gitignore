# dependencias
node_modules/

# build output (generado)
dist/
.astro/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# logs / env
*.log
.env
.env.*

# Local Netlify folder
.netlify
.vercel

# tooling artifacts (capturas de verificación, snapshots de auditoría)
.playwright-mcp/
.impeccable/

# ajustes locales de Claude Code (acumulan comandos de deploy con credenciales — NUNCA commitear)
.claude/settings.local.json

# config sFTP de VS Code (contiene la contraseña de Arsys — NUNCA commitear)
.vscode/sftp.json

# clave de API de Brevo (el .example.php sí se commitea; este NO)
public/api/brevo-config.php

# imágenes sueltas en la raíz (las de la web viven en public/assets/img/)
/*.jpeg
/*.jpg
